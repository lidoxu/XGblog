---
import { allCategories } from '../../features/core/taxonomy';

interface Props {
  counts: Map<string, number>;
  limit?: number;
}

const { counts, limit = 6 } = Astro.props;
const visible = allCategories.slice(0, limit);
---

<section class="sidebar-card list-card" aria-labelledby="category-title">
  <div class="section-heading">
    <h2 id="category-title">文章分类</h2>
    {allCategories.length > limit && <a class="more-link" href="/categories">更多</a>}
  </div>
  <ul>
    {visible.map((category) => (
      <li>
        <a href={`/categories/${category.slug}`}>
          <span>{category.name}</span>
          <small>{counts.get(category.slug) ?? 0}</small>
        </a>
      </li>
    ))}
  </ul>
</section>
