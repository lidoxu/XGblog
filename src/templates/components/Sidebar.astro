---
import type { Post } from '../../features/core/posts';
import { countCategories } from '../../features/core/taxonomy';
import AuthorCard from './AuthorCard.astro';
import CategoryList from './CategoryList.astro';

interface Props {
  posts: Post[];
}

const { posts } = Astro.props;
const categoryCounts = countCategories(posts);
---

<aside class="sidebar" aria-label="博客信息">
  <AuthorCard />
  <CategoryList counts={categoryCounts} />
</aside>
